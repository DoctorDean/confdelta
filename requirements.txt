# MD-Compare core runtime dependencies.
#
# These mirror the [project.dependencies] table in pyproject.toml and are
# kept for convenience (`pip install -r requirements.txt`). For a proper
# install, prefer:  pip install .            (core)
#                   pip install .[all]       (core + optional features)
#                   pip install .[msm]       (core + Markov State Models)

MDAnalysis>=2.4.0
networkx>=2.8
numpy>=1.21.0,<2.0
scipy>=1.7.0
matplotlib>=3.5.0
seaborn>=0.11.0
pandas>=1.3.0
scikit-learn>=1.0.0
tqdm>=4.62.0

# ---------------------------------------------------------------------------
# Optional features (install via extras instead of uncommenting here):
#   pip install .[msm]      -> pyemma            (Markov State Models)
#   pip install .[leiden]   -> python-igraph, leidenalg  (Leiden communities)
#   pip install .[viz]      -> plotly, ipywidgets
#   pip install .[ml]       -> joblib            (resistance classifier)
#   pip install .[dev]      -> pytest, ruff, black, mypy, build, twine
# ---------------------------------------------------------------------------
